const localConfig = {
  baseURLs: {
    apiRoot: "/api",
    memberService: "http://localhost:8081",
    organizationService: "http://localhost:8082",
    boardTaskService: "http://localhost:8080",
  },
};

export default localConfig;
