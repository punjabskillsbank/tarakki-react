import { X } from "lucide-react";
import { useState } from "react";
import { PrimaryButton } from "../../../components/PrimaryButton";
import type { Member, Priority, Section, Task } from "./types";

type Props = { initialSectionId: string | null; sections: Section[]; members: Member[]; onClose: () => void; onCreate: (task: Omit<Task, "id" | "ticketNum">) => void };

export function QuickAddModal({ initialSectionId, sections, members, onClose, onCreate }: Props) {
  const [title, setTitle] = useState(""); const [sectionId, setSectionId] = useState(initialSectionId ?? sections[0]?.id ?? ""); const [assigneeId, setAssigneeId] = useState<string | null>(null); const [priority, setPriority] = useState<Priority>("medium"); const [dueDate, setDueDate] = useState("");
  const create = () => { if (!title.trim()) return; onCreate({ title: title.trim(), description: "", assignee: members.find((member) => member.id === assigneeId) ?? null, dueDate: dueDate || null, priority, status: "open", labels: [], comments: 0, attachments: 0, sectionId }); onClose(); };
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/20 backdrop-blur-[2px]" onClick={(event) => event.target === event.currentTarget && onClose()}><div className="mx-4 w-full max-w-md rounded-xl border border-[#E6E9EF] bg-white p-5 shadow-2xl"><div className="mb-4 flex items-center justify-between"><h3 className="text-[14px] font-semibold text-[#1F2937]">Create Issue</h3><button onClick={onClose} className="rounded p-1 text-[#9CA3AF] transition-colors hover:bg-[#F3F4F6] hover:text-[#1F2937]"><X size={15} /></button></div><input autoFocus value={title} onChange={(event) => setTitle(event.target.value)} onKeyDown={(event) => event.key === "Enter" && create()} placeholder="Issue summary..." className="mb-3 w-full rounded-lg border border-[#E6E9EF] bg-[#F6F7FB] px-3 py-2.5 text-[13px] text-[#1F2937] outline-none transition-colors placeholder:text-[#C4C4C4] focus:border-[#0073EA]" /><div className="mb-5 grid grid-cols-2 gap-2.5"><label className="space-y-1"><span className="text-[11px] font-semibold text-[#6B7280]">Group</span><select value={sectionId} onChange={(event) => setSectionId(event.target.value)} className="w-full rounded-lg border border-[#E6E9EF] bg-[#F6F7FB] px-2 py-1.5 text-[12px] text-[#1F2937] outline-none focus:border-[#0073EA]">{sections.map((section) => <option key={section.id} value={section.id}>{section.name}</option>)}</select></label><label className="space-y-1"><span className="text-[11px] font-semibold text-[#6B7280]">Priority</span><select value={priority} onChange={(event) => setPriority(event.target.value as Priority)} className="w-full rounded-lg border border-[#E6E9EF] bg-[#F6F7FB] px-2 py-1.5 text-[12px] text-[#1F2937] outline-none focus:border-[#0073EA]">{(["critical", "high", "medium", "low", "none"] as Priority[]).map((value) => <option key={value} value={value}>{value}</option>)}</select></label><label className="space-y-1"><span className="text-[11px] font-semibold text-[#6B7280]">Assignee</span><select value={assigneeId ?? ""} onChange={(event) => setAssigneeId(event.target.value || null)} className="w-full rounded-lg border border-[#E6E9EF] bg-[#F6F7FB] px-2 py-1.5 text-[12px] text-[#1F2937] outline-none focus:border-[#0073EA]"><option value="">Unassigned</option>{members.map((member) => <option key={member.id} value={member.id}>{member.name}</option>)}</select></label><label className="space-y-1"><span className="text-[11px] font-semibold text-[#6B7280]">Due date</span><input type="date" value={dueDate} onChange={(event) => setDueDate(event.target.value)} className="w-full rounded-lg border border-[#E6E9EF] bg-[#F6F7FB] px-2 py-1.5 text-[12px] text-[#1F2937] outline-none focus:border-[#0073EA]" /></label></div><div className="flex justify-end gap-2"><button onClick={onClose} className="rounded-lg border border-[#E6E9EF] bg-[#F6F7FB] px-3 py-1.5 text-[12px] text-[#6B7280] transition-colors hover:bg-[#E6E9EF]">Cancel</button><PrimaryButton onClick={create} className="h-auto px-4 py-1.5 text-[12px]">Create</PrimaryButton></div></div></div>;
}
